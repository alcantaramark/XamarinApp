<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:toolkit="clr-namespace:FormsToolkit;assembly=FormsToolkit"
    x:Class="MarkApp.Views.NewDiary"
    Title="New Diary">
    <ContentPage.Content>
        <StackLayout>
            <Entry Placeholder="Location" Text="{Binding Location}" />
            <CollectionView ItemsLayout="HorizontalList" HeightRequest="100">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="35"/>
                                <RowDefinition Height="35"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="140" />
                                <ColumnDefinition Width="140" />
                            </Grid.ColumnDefinitions>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
            <Button Text="Add a Photo" Command="{Binding SelectPhotos}"/>
            <Label Text="Include in Photo Gallery"/>
            <CheckBox IsChecked="{Binding IncludeInPhotoGallery}" />
            <Button Text="Next" Command="{Binding PostData}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
